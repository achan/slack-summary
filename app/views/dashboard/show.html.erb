<% content_for(:title, "Dashboard — Slack Summary") %>

<div class="dashboard">
  <h2>Workspaces</h2>

  <% if @workspaces.empty? %>
    <p class="empty-state">No workspaces configured yet. Add a workspace to get started.</p>
  <% else %>
    <% @workspaces.each do |workspace| %>
      <div class="workspace-card">
        <h3><%= workspace.team_name || workspace.team_id %></h3>
        <ul class="channel-grid">
          <% workspace.channels.active.each do |channel| %>
            <li>
              <%= link_to workspace_channel_path(workspace, channel.channel_id), class: "channel-card" do %>
                <span class="channel-name">#<%= channel.channel_name || channel.channel_id %></span>
              <% end %>
            </li>
          <% end %>
        </ul>
      </div>
    <% end %>
  <% end %>
</div>
