<% content_for(:title, "Add Channel — #{@workspace.team_name}") %>

<div class="max-w-md">
  <h2 class="text-xl font-semibold mb-6">Add Channel</h2>
  <%= render "form", workspace: @workspace, channel: @channel, available_channels: @available_channels %>
</div>
