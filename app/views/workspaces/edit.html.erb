<% content_for(:title, "Edit Workspace — #{@workspace.team_name}") %>

<div class="max-w-md">
  <h2 class="text-xl font-semibold mb-6">Edit Workspace</h2>
  <%= render "form", workspace: @workspace %>
</div>
